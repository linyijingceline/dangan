# 伙伴档案 · 产品需求文档（PRD.md）

> **项目**：伙伴档案（partner-profile）
> **阶段**：Day 4｜第 1 周 · 产品需求定义
> **前置文档**：research.md（Day 3，方向与边界的依据）
> **本文档任务**：定死 MVP 做哪几件事、每件事做到什么程度算合格，供 Day 5-7 开发时对照
> **写作日期**：2026-09-24

---

## 一、项目概述

**一句话定位**（继承自 research.md 第六节）：

> 「伙伴档案」= 让团队成员自己填写信息、系统按人展示成一张张档案、管理员可增删改的轻量档案库。

**背景**：调研发现（research.md），现有方案各有短板——腾讯问卷只有表格视图、填完不能改、没有字段权限；飞书通讯录权限完善但要求整个组织都用飞书；Notion 人员目录形态最接近，但字段级权限要企业版付费。我们要的是：**像 Notion 那样的档案卡展示 + 像飞书那样的字段级权限 + 免费**。

**本期目标**：7 天内交付一个不需要注册的网页，成员自己填、所有人能看基础信息、管理员能维护全部档案。

---

## 二、目标用户与使用场景

**两类用户**：

| 角色 | 是谁 | 核心诉求 |
|---|---|---|
| 团队成员 | 填写自己信息的伙伴 | 填一次就完事，别让我注册 |
| 管理员 | 维护档案的团队负责人 | 谁能看什么我说了算，填错了我要能改 |

**核心场景（三个）**：

1. **第一次填档案（1 分钟）**：成员收到链接 → 打开填写页 → 填完提交 → 看到"提交成功"。
2. **想找某个伙伴（10 秒）**：打开列表页 → 一眼扫过所有人的姓名/专业/学校 → 点进去看详情。
3. **管理员维护（30 秒）**：输口令进管理页 → 改错字、补录没填的人、删除离职/退出的人。

**反面场景（明确不服务）**：想按专业搜索筛选的人、想导出 Excel 的人、想看历史修改记录的人——见第七节本期不做。

---

## 三、MVP 功能需求

优先级：**P0 = 必须有，缺一不可交付**；P1 = 尽量做，做不完按降级方案处理。

### P0-1｜填写并提交档案

**用户动作**：成员打开填写页（一个链接，无需注册登录），填写表单，点「提交」。

**可观察结果**：
- 提交成功提示"提交成功，你的档案已加入"
- 立刻能在列表页看到自己这一条
- 必填项没填时，提交按钮不通过，并在该字段下方显示红色提示文字

**验收标准**：
- [ ] 无需注册、无需登录，打开链接就能填
- [ ] 必填项：姓名、专业、学校、电话、家庭住址（共 5 项），缺任一项无法提交
- [ ] 选填项：生日、爱好、备注，留空也能提交
- [ ] 提交成功后有明确提示，且列表页能立刻查到该条
- [ ] 填写页顶部有一句告知："电话和家庭住址仅管理员可见，不会在公开页面展示"（合规要求）
- [ ] 提交失败（如网络断开）时提示"提交失败，请重试"，已填内容不丢失

### P0-2｜档案列表与详情

**用户动作**：打开列表页，看到所有人的档案卡；点某一张卡片进入详情页。

**可观察结果**：
- 列表页显示一张张档案卡，每张至少含姓名、专业、学校
- 详情页显示该成员的全部非敏感信息
- **电话和家庭住址在列表页和详情页都不显示**（未登录管理员时）
- 列表为空时显示"还没有人填写，快去邀请伙伴吧"

**验收标准**：
- [ ] 列表页展示所有已提交档案，每张卡至少含姓名、专业、学校
- [ ] 点击卡片进入详情页
- [ ] 列表页与详情页均不显示电话、家庭住址
- [ ] **关键安全项**：未通过管理员身份时，用浏览器开发者工具（F12 → Network）查看请求返回的数据，里面**不包含**电话和家庭住址（证明是后端/数据库侧过滤，不是前端隐藏）
- [ ] 手机浏览器打开时卡片纵向排列，不横向溢出
- [ ] 无数据时有空状态提示，不是白屏

### P0-3｜管理员增删改

**用户动作**：管理员在页面输入口令 → 进入管理页 → 新增 / 编辑 / 删除任一档案。

**可观察结果**：
- 口令错误时提示"口令错误"，不进入管理页
- 管理页中能看到每条档案的电话和家庭住址
- 删除某条后，列表页立刻少一条；刷新页面仍然是少的
- 编辑保存后，列表页显示的是新内容

**验收标准**：
- [ ] 有管理员口令入口，口令错误无法进入
- [ ] 管理页可见电话、家庭住址两项敏感字段
- [ ] 可新增一条档案（不必走填写页）
- [ ] 可编辑任一条档案的任何字段
- [ ] 可删除任一条档案，删除前有一次确认（防止误删）
- [ ] 增、删、改的结果在列表页立即可见，刷新后依然生效
- [ ] 口令不存在于前端代码、日志或任何仓库文件中（只存在于环境变量）

> **降级方案**：若管理员登录做不完，降级为"管理页仅在本机打开时才显示"（靠环境变量开关），但**敏感字段的数据库侧权限不能降级**。

---

## 四、页面结构

| 页面 | 路径（示意） | 内容 | 谁能进 |
|---|---|---|---|
| 填写页 | `/submit.html` | 表单 + 告知文字 + 提交按钮 | 所有人 |
| 列表页 | `/`（首页） | 档案卡列表 + 顶部"我来填写"入口 | 所有人 |
| 详情页 | `/profile.html?id=xxx` | 一个人的完整非敏感信息 | 所有人 |
| 管理页 | `/admin.html` | 口令入口 + 档案表格（含敏感字段）+ 增删改 | 仅管理员 |

**导航关系**：列表页是首页，顶部有"我来填写"按钮直达填写页；点卡片进详情页；页脚有低调的管理员入口。

---

## 五、数据字段表

| 字段 | 类型 | 必填 | 可见范围 | 说明 |
|---|---|---|---|---|
| 姓名 | 文本 | ✅ | 所有人 | |
| 专业 | 文本 | ✅ | 所有人 | |
| 学校 | 文本 | ✅ | 所有人 | |
| 电话 | 文本 | ✅ | **仅管理员** | 敏感字段，存敏感表 |
| 家庭住址 | 文本 | ✅ | **仅管理员** | 敏感字段，存敏感表 |
| 生日 | 日期 | 选填 | 所有人 | |
| 爱好 | 文本 | 选填 | 所有人 | |
| 加入时间 | 日期时间 | 自动生成 | 所有人 | 提交时自动记录 |
| 备注 | 多行文本 | 选填 | **仅管理员** | 管理员内部备注 |

**字段规则说明**：
- 电话、家庭住址按 Day 3 决策**保持必填**，理由是团队联络确实需要；前提是**填写前已与伙伴沟通并获得同意**，且填写页有明确告知。
- 公开表与敏感表**分开存储**：公开表放姓名/专业/学校/生日/爱好/加入时间，敏感表放电话/家庭住址/备注。读取权限设在数据库侧。
- 本 MVP 不做头像上传，用姓名首字生成占位头像。

---

## 六、权限与隐私设计（★ 本项目的生命线）

| 要求 | 实现方式 | 不能怎么做 |
|---|---|---|
| 电话、住址仅管理员可见 | 敏感表在数据库侧的读权限只对管理员身份开放 | ❌ 不能只靠前端把号码显示成 `138****0000` |
| 管理员身份校验 | 口令存环境变量，服务端/数据库规则校验 | ❌ 不能把口令写在网页代码里 |
| 收集前告知 | 填写页顶部一行说明 | ❌ 不能什么都不说就收 |
| 真实数据不进仓库 | `.gitignore` 已拦 `.env` 与 `*.csv`；示例文件用假数据 | ❌ 不能用真实姓名电话做演示 |

> 为什么反复强调：数据一旦传到浏览器，按 F12 就能看到原文，前端"隐藏"等于已经泄露。调研中飞书、Notion 都用字段级权限来解决，我们同理。

---

## 七、本期不做（完整清单）

功能类：账号体系与多管理员、成员自助修改自己已提交的信息、头像上传、搜索与标签筛选、导出 Excel/CSV、消息通知、多团队切换、修改留痕与历史版本、点赞评论、数据统计图表、分页（列表一次全部展示，人数超过 100 再考虑）。

> 开发期间若想加新功能，必须先回到本文档修订本节并说明理由，不允许"顺手就做了"。

---

## 八、非功能需求

| 类别 | 要求 |
|---|---|
| 匿名访问 | 填写与查看全程无需注册/登录 |
| 性能 | 列表页首屏加载 ≤ 3 秒 |
| 移动适配 | 手机浏览器可用（卡片纵向排列），不做原生 App |
| 合规 | 填写页告知信息用途；页脚标注"信息仅团队内部使用" |
| 容错 | 提交失败保留已填内容；单个页面报错不白屏，给出文字提示 |
| 成本 | 全部使用免费额度，不付费 |

---

## 九、MVP 总验收标准（逐条打勾）

- [ ] 打开填写页能填，必填 5 项校验生效
- [ ] 提交后列表页立刻出现该条档案
- [ ] 列表页以卡片形式展示所有人，含姓名/专业/学校
- [ ] 点卡片进详情页，显示完整非敏感信息
- [ ] **未登录管理员时，浏览器收到的原始数据里没有电话和住址**（F12 → Network 验证）
- [ ] 管理员输错口令进不去
- [ ] 管理页能看到电话、住址，并能增删改
- [ ] 删除有二次确认，删除后刷新页面依然是少的
- [ ] 填写页有信息用途告知文字
- [ ] 手机浏览器打开布局正常
- [ ] 首屏加载 ≤ 3 秒
- [ ] 全程无需注册登录
- [ ] 仓库里没有任何真实档案数据

---

## 十、开放问题（移交 Day 5）

| 问题 | 影响 | 谁来定 |
|---|---|---|
| 托管数据库选 Supabase / 腾讯云开发 CloudBase / LeanCloud 哪一家 | 整个技术方案 | Day 5 选型 |
| 敏感表的读权限具体怎么写（行级 / 列级 / 视图） | P0-2 的安全性 | Day 5 设计 |
| 管理员口令校验放在哪一层 | P0-3 的实现方式 | Day 5 设计 |
| 免费额度是否够用、超出怎么办 | 长期可用性 | Day 5 核实 |
| 重复提交怎么处理（同一人填两次） | 数据整洁度 | Day 4 决策：**本期不做查重，由管理员删除重复项** |

---

## 十一、Day 4 自检

| 检查项 | 结果 |
|---|---|
| 每个功能都写清了用户动作和可观察结果 | ✅ 三个功能均有 |
| 每个功能有可勾选的验收标准 | ✅ 共 21 条 |
| 列出了页面/视图清单 | ✅ 第四节 |
| 列出了数据字段及必填/可见范围 | ✅ 第五节 |
| 列出了本期不做 | ✅ 第七节，与 research.md 一致 |
| 未越界做技术选型 | ✅ 数据库选型留给 Day 5 |
| 敏感字段有明确实现方式 | ✅ 第六节 |

**下一步**：Day 5 技术选型（TECH_DESIGN.md）。
