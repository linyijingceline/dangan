# 伙伴档案 · 技术设计文档（TECH_DESIGN.md）

> **项目**：伙伴档案（partner-profile）
> **阶段**：Day 5｜技术选型与方案设计
> **前置文档**：PRD.md（Day 4）、research.md（Day 3）
> **本文档任务**：定死"用什么技术、数据怎么存、前后怎么连"，供 Day 6-7 开发时照着做
> **写作日期**：2026-09-24
> **⚠️ 待确认**：本文按「腾讯云开发 CloudBase」编写（Day 5 推荐方案）。若改选 Supabase，只需替换第 3、4、8 节，其余结构不变。

---

## 一、技术路线（已选定）

**静态网页 + 托管数据库（CloudBase）+ 少量云函数**

| 组成部分 | 用什么 | 说明 |
|---|---|---|
| 页面 | 纯 HTML/CSS/JS，无框架 | 越简单越好，7 天内跑得起来 |
| 网页托管 | CloudBase 静态托管（免费额度 1GB） | 国内访问快；**可以不用 GitHub Pages**，仓库因此不必公开 |
| 数据 | CloudBase 云数据库（文档型，两个集合） | 每月 3000 资源点内够用 |
| 权限与写入 | CloudBase 云函数（Node.js） | 敏感字段在云函数里过滤，浏览器永远拿不到 |
| 前端连后端 | CloudBase JS SDK 的 `callFunction` | 不自己搭接口、不管域名和证书 |

### 方案对比：为什么放弃纯前端方案

| 维度 | 方案 A：纯静态页 + 浏览器本地存储 | 方案 B（选定）：CloudBase 静态页 + 云数据库 + 云函数 |
|---|---|---|
| 学习成本 | 低，几乎不用学新东西 | 中，要学云函数与数据库的基本操作（约半天） |
| 线上持久化 | ❌ 数据只存在各自浏览器，**队友看不到别人填的** | ✅ 数据集中存在云端，所有人看到同一份 |
| 费用 | 0 | 0（免费额度内） |
| 排错难度 | 低，但出问题只能清缓存重来 | 中，有控制台日志可查 |
| 字段级权限 | ❌ 做不到（数据都在浏览器里） | ✅ 云函数过滤，数据根本不下发 |

**放弃 A 的核心理由**：A 不是"简单一点"，而是**根本实现不了 P0-1 和 P0-2**——队友各自填的档案存在各自的电脑里，永远聚不到一块，更别说"电话仅管理员可见"。

---

## 二、系统结构（数据怎么走）

```
成员浏览器 ──填写──▶ 云函数 submitProfile ──▶ 云数据库
                                              ├ profiles_public（姓名/专业/学校/生日/爱好）
                                              └ profiles_private（电话/住址/备注）

任何人浏览器 ──看列表──▶ 云函数 listProfiles ──▶ 只读 profiles_public ──▶ 返回公开字段
                                                  （profiles_private 碰都不碰）

管理员浏览器 ──输口令──▶ 云函数 adminVerify ──▶ 口令正确才放行
                     ──看全部──▶ 云函数 adminList ──▶ 同时读两张表 ──▶ 返回含敏感字段
                     ──改/删───▶ 云函数 adminUpdate / adminDelete ──▶ 校验口令后写库
```

**关键点**：数据库的两个集合都设置为「**仅管理端可读写**」。这样浏览器里的 JS SDK **直接读库会被拒绝**，只能走云函数。云函数跑在服务端，它决定返回哪些字段——敏感字段不下发，队友按 F12 也看不到。

---

## 三、项目结构

```
partner-profile/
├─ index.html            列表页（首页）：档案卡 + "我来填写"入口
├─ submit.html           填写页：表单 + 告知文字
├─ profile.html          详情页：一个人的公开信息
├─ admin.html            管理页：口令入口 + 表格 + 增删改
├─ css/
│  └─ style.css          统一样式（手机适配在这里）
├─ js/
│  ├─ cloudbase.js       初始化 SDK、封装 callFunction
│  ├─ list.js            列表页逻辑
│  ├─ submit.js          填写页逻辑与必填校验
│  ├─ profile.js         详情页逻辑
│  └─ admin.js           管理页逻辑
├─ functions/            云函数（部署到云端，不随网页发布）
│  ├─ submitProfile/     提交档案
│  ├─ listProfiles/      公开列表
│  ├─ getProfile/        单条公开详情
│  ├─ adminVerify/       校验管理员口令
│  ├─ adminList/         管理员看全部（含敏感字段）
│  ├─ adminUpdate/       管理员改
│  └─ adminDelete/       管理员删
├─ .env.example          需要准备什么（值全是假的）
├─ .gitignore            .env 与数据文件不进仓库
├─ README.md
├─ research.md
├─ PRD.md
└─ TECH_DESIGN.md        本文
```

---

## 四、数据对象与字段

按 PRD 第五节，公开信息与敏感信息**分两个集合存**（双保险：即使某个云函数写错，也漏不出敏感字段）。

### 集合 1：`profiles_public`（公开）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `_id` | string | 自动 | 档案唯一编号 |
| `name` | string | ✅ | 姓名 |
| `major` | string | ✅ | 专业 |
| `school` | string | ✅ | 学校 |
| `birthday` | string | 选填 | 生日，格式 YYYY-MM-DD |
| `hobby` | string | 选填 | 爱好 |
| `joinedAt` | date | 自动 | 提交时间，服务端生成 |
| `createdAt` | date | 自动 | 记录创建时间 |

### 集合 2：`profiles_private`（敏感）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `_id` | string | 自动 | 本表主键 |
| `profileId` | string | ✅ | 对应 `profiles_public._id` |
| `phone` | string | ✅ | 电话 |
| `address` | string | ✅ | 家庭住址 |
| `note` | string | 选填 | 管理员备注 |

### 权限设置（必须做，写在部署清单里）

| 集合 | 数据库权限 | 效果 |
|---|---|---|
| `profiles_public` | 仅管理端可读写 | 浏览器直接读 → 拒绝；只能通过云函数读 |
| `profiles_private` | 仅管理端可读写 | 同上，且只有云函数能碰 |

---

## 五、云函数清单（API 列表）

| 云函数 | 谁可以调 | 入参 | 成功返回 | 失败返回 |
|---|---|---|---|---|
| `submitProfile` | 任何人 | name, major, school, birthday?, hobby?, phone, address | `{ ok: true, id }` | `{ ok: false, msg: "姓名不能为空" }` |
| `listProfiles` | 任何人 | 无 | `{ ok: true, list: [{ id, name, major, school, birthday, hobby, joinedAt }] }` | `{ ok: false, msg }` |
| `getProfile` | 任何人 | id | `{ ok: true, profile: { 公开字段 } }` | `{ ok: false, msg: "档案不存在" }` |
| `adminVerify` | 任何人（试试口令） | pass | `{ ok: true }` / `{ ok: false, msg: "口令错误" }` | 同左 |
| `adminList` | 需带口令 | pass | `{ ok: true, list: [{ 公开字段 + phone, address, note }] }` | `{ ok: false, msg: "口令错误" }` |
| `adminUpdate` | 需带口令 | pass, id, 要改的字段 | `{ ok: true }` | `{ ok: false, msg }` |
| `adminDelete` | 需带口令 | pass, id | `{ ok: true }` | `{ ok: false, msg }` |

**关于管理员身份（MVP 简化方案）**：管理类云函数每次调用都带上 `pass`，云函数内部与环境变量里的 `ADMIN_PASS` 比对。优点是零依赖、好排错；缺点是口令每次都在请求里传。**前提：必须用 HTTPS（CloudBase 默认提供）**。口令正确才返回敏感字段。

---

## 六、数据流（三个核心场景）

**场景 1：成员填写**
1. 打开 `submit.html` → 前端校验 5 个必填项 → 不通过就在字段下方标红
2. 调用 `submitProfile`
3. 云函数再次校验必填（**前端校验可以绕过，服务端必须再校验一次**）
4. 写 `profiles_public` 拿回 `_id` → 用这个 id 写 `profiles_private`
5. 返回成功 → 页面提示"提交成功"

**场景 2：任何人看列表**
1. 打开 `index.html` → 调用 `listProfiles`
2. 云函数只查 `profiles_public`，按 `joinedAt` 倒序
3. 前端渲染成一张张卡片
4. 点卡片 → 调 `getProfile` → 跳 `profile.html?id=xxx`

**场景 3：管理员维护**
1. 打开 `admin.html` → 输口令 → `adminVerify` 通过才显示表格
2. `adminList` 拿全部字段（含敏感）
3. 改 → `adminUpdate`；删 → 二次确认 → `adminDelete`
4. 删除时**两张表的记录一起删**，避免留下孤儿数据

---

## 七、错误处理

| 情况 | 怎么处理 | 用户看到什么 |
|---|---|---|
| 必填项没填 | 前端先拦；云函数再拦一次 | 字段下方红字："这项必填" |
| 网络断开 / 云函数调用失败 | 前端 catch，保留已填内容 | "网络好像不太好，请重试"（**不清空表单**） |
| 数据库写入失败 | 云函数捕获，返回 ok:false | "提交失败，请稍后再试" |
| 未授权读敏感字段 | 数据库权限直接拒绝 + 云函数不返回 | 列表/详情里本来就不显示这两项 |
| 口令错误 | 云函数返回 ok:false | "口令错误"，不进入管理页 |
| 查不到这条档案 | 返回"档案不存在" | 详情页显示"这条档案不存在或已被删除" |
| 云函数报错（未捕获） | 控制台有日志；前端统一提示 | "出了点问题，请稍后再试"（**不把技术错误直接甩给用户**） |

---

## 八、环境变量与配置

| 变量/配置 | 放哪 | 说明 |
|---|---|---|
| `CLOUBASE_ENV_ID` | 网页里（本来就是公开的） | CloudBase 环境 ID，不是秘密 |
| `ADMIN_PASS` | **云函数的环境变量**，绝不放网页 | 管理员口令 |
| 数据库权限 | CloudBase 控制台 | 两个集合都设"仅管理端可读写" |
| 安全域名 | CloudBase 控制台 | 把静态托管域名加入白名单，否则浏览器调不了云函数 |
| `.env`（本地） | 不进仓库 | 本地调试时放这些值，`.env.example` 里全是假数据 |

> 云函数运行在服务端，**不需要**像 Supabase 那样在网页里放匿名 key，这一点反而是 CloudBase 更安全的优势。

---

## 九、部署与迁移注意事项

**部署顺序（第一次）**
1. CloudBase 控制台创建环境（选免费版），记下环境 ID
2. 建两个集合 `profiles_public`、`profiles_private`
3. 把两个集合权限设为「仅管理端可读写」
4. 部署 7 个云函数（CloudBase CLI 或控制台上传）
5. 给云函数设置环境变量 `ADMIN_PASS`
6. 上传网页文件到静态托管
7. 把静态托管域名加入安全域名白名单
8. 用一条假数据（张三 / 13800000000）走一遍完整流程

**要注意的坑**
- **免费环境每 6 个月要续期一次**，到期前 1 个月内可续，设个提醒
- 资源点是**共享池**：数据库、云函数、流量一起算，别写死循环查询
- 静态托管默认域名可直接用；绑自己的域名在国内需要备案，本期不做
- 云函数改动后要**重新部署**才生效，网页改动也要重新上传

**以后想迁移（重要）**
- CloudBase 用的是**文档型数据库**，和 Supabase 的 PostgreSQL 不通用
- 为降低锁定：所有读写都经过云函数，**不在网页里写死任何数据库查询语句**——将来换数据库，只改云函数，网页一行不动
- 真要搬家：用云函数导出 JSON，再导入新库

---

## 十、风险与开放问题

| 风险/问题 | 影响 | 什么时候解决 |
|---|---|---|
| 免费额度到底够不够（3000 点/月） | 长期可用性 | Day 6 部署后观察控制台用量 |
| 云函数会不会因为没人访问而冷启动变慢 | 首次打开略慢 | 实测，可接受就不管 |
| 口令每次请求都传，是否够安全 | 安全性 | MVP 可接受（HTTPS 已加密）；Day 8+ 可换成登录态 token |
| 同一人重复提交 | 数据重复 | 按 PRD 决策：本期不做查重，管理员手动删 |
| 队友填写前是否都已知情同意 | 合规 | 你确认（填写页有告知文字） |

---

## 十一、Day 5 自检

| 检查项 | 结果 |
|---|---|
| 比较了两套方案并说明推荐理由 | ✅ 第一节 |
| 项目结构 | ✅ 第三节 |
| 数据对象及字段（含敏感字段隔离） | ✅ 第四节 |
| API（云函数）列表 | ✅ 第五节 |
| 数据流 | ✅ 第六节 |
| 错误处理 | ✅ 第七节 |
| 环境变量 | ✅ 第八节 |
| 部署和迁移注意事项 | ✅ 第九节 |
| 没有直接编造实现（未写代码） | ✅ 只有结构与约定 |

**下一步**：Day 6 开发（按本文建库、写云函数、做页面）。
